"""
SeisCodec linear-probing experiments.

Place this folder inside your SeisCodec project root (next to model.py),
then run any probe from either:

    # from project root:
    python probes/probe_binary.py --seis_codec_ckpt logs/.../ckpt

    # from inside probes/:
    cd probes
    python probe_binary.py --seis_codec_ckpt ../logs/.../ckpt

    # as a module:
    python -m probes.probe_binary --seis_codec_ckpt logs/.../ckpt
"""
